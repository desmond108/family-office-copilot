[pytest]
# Meridian Family Office Copilot — test suite.
# Runs fully offline in DEMO_MODE (no API key, no network, no billing).
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
