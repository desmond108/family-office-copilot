# Family Office Copilot — Streamlit app (app.py)
# Demo/POC mode (DEMO_MODE=1, the default) needs these:
streamlit>=1.40
pandas>=2.0
python-pptx>=1.0   # generate the Proposal deck as PowerPoint
reportlab>=4.0     # generate the Proposal deck as PDF
pypdf>=4.0         # v9: read text from research/other PDF uploads (pure-python)
python-docx>=1.1   # v9: read text from research/other .docx uploads (pure-python)

# Optional — required ONLY when DEMO_MODE=0 (enables live data + the live model).
# Uncomment these and add the keys (see .streamlit/secrets.toml.example) to turn them on:
# yfinance>=0.2.40   # live prices + fund fields
# certifi>=2024.0    # TLS roots for the FRED HTTPS call in datafeed.py
# anthropic>=0.40    # v10: the Proposal page's "Generate with Claude" button (narrative.py)
#                    # runs the portable prompt against the live Claude API; also powers the
#                    # grounded phrasing in "Ask the book". Without it / in DEMO_MODE both
#                    # degrade to a deterministic grounded summary — no model call. (The
#                    # copy-and-paste prompt on the Proposal page works in any LLM regardless.)
